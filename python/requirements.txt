requirements.txt
# Allan Variance Studio — Python Analysis Pipeline
# Requirements for IEEE Std 1293-2018 compliant Allan variance characterization
# and Parkinsonian tremor detection pipeline
# 
# Install: pip install -r requirements.txt
# Tested on: Python 3.9, 3.10, 3.11, 3.12

# ========================================================================
# CORE NUMERICAL & SCIENTIFIC COMPUTING
# ========================================================================
numpy>=2.5.1,<3.0.0          # Array operations, FFT, signal processing foundation
scipy>=1.10.0                 # Signal processing, filtering, optimization, statistics

# ========================================================================
# DATA HANDLING & I/O
# ========================================================================
pandas>=2.0.0                 # DataFrame handling for sensor recordings and clinical metadata
openpyxl>=3.1.0               # Excel export for clinical reports
h5py>=3.8.0                   # HDF5 binary sensor data I/O
pyarrow>=12.0.0               # Parquet format for efficient storage

# ========================================================================
# SIGNAL PROCESSING & ALLAN VARIANCE
# ========================================================================
# Allan variance computation — IEEE Std 1293-2018 compliant
allantools>=2019.9            # Standard Allan deviation, modified Allan, Hadamard variance
# Alternative (more performant): 
# allan-variance>=0.1.0       # C-accelerated implementation (optional)

# Spectral analysis
spectrum>=0.8.1               # PSD estimation, Welch periodogram, Lomb-Scargle

# ========================================================================
# MACHINE LEARNING & CLASSIFICATION
# ========================================================================
scikit-learn>=1.9.0           # Random Forest, SVM, cross-validation, metrics
joblib>=1.3.0                 # Parallel processing for model training

# Optional deep learning (for advanced tremor classification)
# tensorflow>=2.13.0          # Neural network models (uncomment if needed)
# torch>=2.0.0                # PyTorch alternative (uncomment if needed)

# ========================================================================
# VISUALIZATION & REPORTING
# ========================================================================
matplotlib>=3.7.0             # Allan deviation plots, PSD plots, time series
seaborn>=0.13.2               # Statistical visualizations, heatmaps
plotly>=5.15.0                # Interactive web-based plots for clinical dashboard

# ========================================================================
# STATISTICAL ANALYSIS
# ========================================================================
statsmodels>=0.14.6           # Regression, mixed-effects models, hypothesis testing
pingouin>=0.6.1               # Advanced statistics (Bland-Altman, ICC, reliability)

# ========================================================================
# CLINICAL DATA & REPORTING
# ========================================================================
reportlab>=5.0.0              # PDF report generation for clinical protocols
fpdf2>=2.7.0                  # Alternative PDF generation (lighter weight)

# ========================================================================
# CONFIGURATION & LOGGING
# ========================================================================
pyyaml>=6.0                   # YAML config file parsing
python-dotenv>=1.2.2          # Environment variable management
loguru>=0.7.3                 # Structured logging (replaces standard logging)

# ========================================================================
# TESTING & QUALITY ASSURANCE
# ========================================================================
pytest>=9.1.1                 # Unit testing framework
pytest-cov>=7.1.0             # Code coverage reporting
pytest-xdist>=3.3.0           # Parallel test execution
hypothesis>=6.82.0              # Property-based testing for numerical stability

# ========================================================================
# DEVELOPMENT TOOLS (optional, for contributors)
# ========================================================================
# black>=23.7.0               # Code formatting
# flake8>=6.1.0               # Linting
# mypy>=1.5.0                 # Static type checking
# jupyter>=1.0.0              # Interactive notebooks for analysis
# ipykernel>=6.25.0           # Jupyter kernel

# ========================================================================
# CLOUD & DEPLOYMENT (for production pipeline)
# ========================================================================
# google-cloud-storage>=2.10.0  # GCS upload for Bangladesh deployment
# boto3>=1.28.0                 # AWS S3 alternative
# requests>=2.31.0              # HTTP API calls
# fastapi>=0.101.0              # REST API for analysis service
# uvicorn>=0.23.0               # ASGI server for FastAPI

# ========================================================================
# NOTEBOOK & INTERACTIVE ANALYSIS
# ========================================================================
# Uncomment for Jupyter-based analysis workflows
# jupyterlab>=4.0.0
# ipywidgets>=8.1.0
# nbformat>=5.9.0

# ========================================================================
# SYSTEM-SPECIFIC NOTES
# ========================================================================
# 
# For Apple Silicon (M1/M2/M3 Macs):
#   pip install --no-cache-dir -r requirements.txt
#   Some packages may need: brew install libomp
#
# For Windows:
#   Install Microsoft C++ Build Tools first for compiled extensions
#
# For Linux (Ubuntu/Debian):
#   sudo apt-get install libhdf5-dev libopenblas-dev
#
# For Bangladesh deployment (low-bandwidth):
#   pip install --no-deps -r requirements.txt  # Then resolve manually
#   Or use: pip download -r requirements.txt -d ./packages  # For offline install
